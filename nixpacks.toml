[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install"]

[phases.build]
cmds = [
  "pnpm --filter @latent-space/web build",
]

[start]
cmd = "cd apps/server && node_modules/.bin/tsx src/index.ts"
